interface:
  display_name: "Architecture Baseline"
  short_description: "Record cross-feature architecture decisions"
  default_prompt: "Use $architecture-baseline for one explicitly authorized full, lite, recover, resolve-spike, amend, or approve operation."

policy:
  allow_implicit_invocation: false
