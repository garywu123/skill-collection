interface:
  display_name: "Architecture Baseline"
  short_description: "Record cross-feature architecture decisions"
  default_prompt: "Use $architecture-baseline for one explicitly authorized baseline, recovery, amendment, or approval operation."

policy:
  allow_implicit_invocation: false
