interface:
  display_name: "Delivery Gates"
  short_description: "Review and record human delivery gates"
  default_prompt: "Use $delivery-gates for exactly one requested accept-feature, record-feature-decision, release, authorize-release, or record-release-result operation, then stop."

policy:
  allow_implicit_invocation: false
